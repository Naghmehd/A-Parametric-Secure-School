<div class="container">
  <h1>All the Courses </h1>
    <div class="row">
      <div class="col-md-8 courses-box">
        <ol>
        <% @courses.each do |course| %>
          <li>
            <p><%= course.name %></p>
            <p><%= course.duration %></p>

            <%= link_to "Student's courses", school_student_path(@teacher, student) %>

          </li>
          <%end%>
        </ol>
      </div>
</div>
